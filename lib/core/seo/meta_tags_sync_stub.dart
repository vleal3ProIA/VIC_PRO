/// Stub no-op para mobile/desktop. La implementación real está en
/// `meta_tags_sync_web.dart` y se selecciona via conditional import.
void applyMetaTags({
  required String title,
  required String description,
  required String siteName,
  String? ogImageUrl,
  String? canonical,
}) {
  // no-op
}
